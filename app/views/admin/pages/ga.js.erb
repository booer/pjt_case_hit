(function(){
  var $loginForm = $('#myModal');
  var $form = $('<%= j render 'ga' %>');

  if ($loginForm.size() > 0) {
    $loginForm.modal();
  } else {
    $('body').append($form);
    $form.modal();
  }
})();